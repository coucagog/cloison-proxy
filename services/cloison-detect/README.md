# CLOISON — services/cloison-detect

Dossier de structure (STACK-0). Le contenu arrive avec le STACK qui le porte :
- `services/cloison-detect` : NER lourd Python — STACK-6.
- `bench/cloison-bench` : harnais de benchmark + scoring — STACK-1.
- `proto/` : contrats protobuf — dès STACK-2/3.
- `deploy/` : Dockerfiles, compose, Caddy/Traefik, SBOM — STACK-0 partiel, complété STACK-3/7.
