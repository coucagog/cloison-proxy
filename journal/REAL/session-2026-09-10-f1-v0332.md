# Session 10/09/2026 — v0.3.3.2 « reasoning_content géré » (F1) + F2/F3 + note I1

> Suite de `consolidation-omarchy-2026-09-09.md`. Arbitrages d'ouverture actés,
> implémentation F1 terminée et testée en local, docs F3 écrites, note I1
> rédigée. Release et re-sonde : voir le plan R1/R2 en fin de document.

---

## 1. Arbitrages d'ouverture (consolidation §5) — ACTÉS

| # | Question | Décision |
|---|---|---|
| 1 | Seuil NER défaut (F2) | **0,70** (aligné code + install + docs ; leurs mesures code plaidant pour 0,70) |
| 2 | Périmètre v0.3.3.2 | **F1 seul** (F5 lexique → v0.3.4) |
| 3 | Roadmap | **F4 `/v1/responses` d'abord**, puis F7 fenêtres parallèles, F6 dernier |
| 4 | Date R2 (session conjointe Omarchy) | **après acceptation v0.3.3.2** (scénario opencode multi-tours thinking) |
| 5 | Réponse client I1 | **je rédige, le pilote valide et envoie** — brouillon : `note-incident-I1-0910.md` |

## 2. F1 — implémenté (code)

**Aller** (`crates/cloison-proxy/src/openai.rs`) :
- `ChatMessage.reasoning_content: Option<String>` — champ **connu** (avant :
  champ inconnu de serde, donc **silencieusement écarté** à l'aller — correction
  d'honnêteté sur le diagnostic I1(a), voir §4) ;
- tokenisé dans `engine.rs::tokenize_chat_request` exactement comme `content`
  (sidecar detect + light_ner + session, même chemin `tokenize_with_detect`).

**Retour non-stream** (`openai.rs` + `engine.rs`) :
- `ResponseMessage.reasoning_content` + restauration fail-loud dans
  `restore_chat_response` (`apply_restore`, marqueur neutre si jeton non
  résolu, compteurs agrégés).

**Retour stream** (`stream.rs`) :
- nouveau canal `delta.reasoning_content` : `BufferAndScan` dédié (mêmes
  règles de confirmation/restauration bornée que `content`), routage dans
  `process_payload`, `reasoning_event` (shape `delta.reasoning_content`),
  clôture fail-loud (`finish`) incluse dans les compteurs du flux.
- un chunk mixte `content` + `reasoning_content` route les deux canaux ;
  un chunk sans rien de routé passe tel quel (comportement inchangé).

**Audit observe-only** (`handlers.rs`) :
- `audit_count_request` compte désormais `reasoning_content` (le comptage de
  réponse était déjà récursif sur toutes les chaînes JSON).

## 3. F2 — seuil NER 0,70 (code)

- `config.rs` : `LightNerConfig::default().threshold` **0,50 → 0,70** et
  `env_f64("CLOISON_NER_THRESHOLD", 0.50)` → **0.70** (les deux endroits
  relevés par l'écart E2) ; commentaire du champ mis à jour (décision du
  09/09, consolidation §3-F2).
- Déjà cohérents, vérifiés : `install-n0.sh:163`, `install-n0.ps1:141`,
  `provision_ner_lite.sh:134`, `docs/CONFIG.md:43`, `docs/N0.md:121`.
- NB : `CLOISON_QUASI_ID_THRESHOLD` reste à 0,50 (jauge quasi-id, sans rapport).

## 4. Correction d'honnêteté sur I1(a)

Le diagnostic client « CLOISON transmet reasoning_content dans les deux sens »
était exact pour le **retour** (deltas SSE pass-through, 22 chunks vérifiés)
mais pas pour l'**aller** : `ChatMessage` sans `extra` flatten → serde écartait
silencieusement `reasoning_content` des requêtes. Le « multi-tour curl avec
replay → 200 » du client était donc nécessairement hors CLOISON. En v0.3.3.1,
**même un client conforme** (qui rejoue le champ) aurait reçu le 400.
v0.3.3.2 corrige : le champ est connu, transmis, et uniquement sous forme de
jetons. Le 400 restant côté opencode (non-replay du SDK) demeure à signaler à
opencode — consigné dans la note I1 §2.2.

## 5. Tests (Windows GNU local, cargo 1.98.0)

- **lib : 26/26 ✅** (inchangés) ;
- **e2e : 15/15 ✅** — 12 existants **inchangés et verts** + 3 nouveaux :
  `reasoning_non_stream_roundtrip` (aller tokenisé — zéro clair côté
  fournisseur, raisonnement inclus — et restauration exacte),
  `reasoning_stream_roundtrip_reassembles_split_sentinels` (sentinelles
  coupées au milieu des deltas reasoning → réassemblées, zéro fuite),
  `reasoning_stream_truncated_sentinel_redacts_at_closure` (résidu rougeoyé
  d'un bloc — parité exacte avec le canal content) ;
- **e2e_n0 : 8/8 ✅**, **e2e_audit : 7/7 ✅**, **e2e_control : 7/7 ✅**.

Leçon du test tronqué : à la clôture, si UNE sentinelle du résidu est non
résolue, **tout le résidu** part au marqueur neutre (fail-loud conservateur,
déjà le comportement du canal content) — assertion de test alignée, code non
modifié.

## 6. F3 — docs (écrites, déploiement à faire avec le cycle)

- **FAQ** (`deploy/docs-site/faq.html`) : nouvelle **Q·9** « Comment activer le
  mode observe-only (audit) ? » — encodages exacts de `env_bool`
  (`1|true|yes|on` → observe-only ; `0|false|no|off|absent` → masquage actif),
  avertissement « jamais sur un flux à protéger » ; entrée TOC ajoutée.
- **Manuel N0 public** (`deploy/docs-site/manuel-n0.html`, nouveau) : coquille
  canonique, 7 sections (ce que N0 est / installation épinglée / configuration
  env / les modes / limites honnêtes / sonde de vérification / coffre
  sauvegarde-restauration — matière R5). Lien « Manuel N0 » ajouté au groupe
  **Déployer** de la sidebar des 10 pages du site (manuel.html exempt : coquille
  différente, sans sidebar docs).
- ⚠️ Leçon de session : les scripts PowerShell `Get-Content`/`Set-Content`
  **double-encodent** les fichiers UTF-8 (lecture ANSI) — repasser par
  `[System.IO.File]::ReadAllText/WriteAllText` avec encodage explicite. Les 10
  fichiers touchés ont été restaurés via git puis refaits byte-safe, vérifiés
  zéro double-encodage.

## 7. Note client I1

Brouillon `journal/REAL/note-incident-I1-0910.md` (statut BROUILLON, à valider
par le pilote avant envoi) : diagnostic précisé (§2, correction d'honnêteté
§4 incluse), correction v0.3.3.2, conséquence d'usage (client qui rejoue
reasoning = multi-tours OK ; défaut opencode à signaler), plan de re-test R2.

## 8. Reste de la session (R1–R2)

1. ~~Commit local + bundle → wonkom : portes `cargo test --locked` (workspace) +
   `e2e_n0`, builds linux-gnu + windows-gnu (rustdev docker)~~ **FAIT** ;
2. ~~Release **v0.3.3.2**~~ **FAITE — publiée le 09/09 soir (UTC)** ;
3. ~~Rebuild edge pin v0.3.3.2 + redeploy `demo-cloison` + re-sonde~~ **FAIT —
   SONDE VERTE** ;
4. Sonde **reasoning** dédiée (roundtrip thinking réel) puis proposition de
   date R2 au client — **préparée** (voir §9).

## 9. Résultats du cycle R1 (vérifiés en réel)

- **Commit `6d45332`** poussé sur `github.com/coucagog/cloison` main (via
  bundle → wonkom → push origin, procédure habituelle).
- **Portes wonkom** (rustdev, Linux natif, `/tmp/rel-v0332-tests.log`) :
  core **99+17**, proxy **26 lib + e2e 15 + e2e_audit 7 + e2e_control 7 +
  e2e_n0 8**, ledger 24+18, audit 19+17, control 5+24, verify 19, cli 3+1,
  wasm 1 — **tout vert, dont les 3 nouveaux tests reasoning sur Linux**.
  (Le `tail -8` du script n'affiche que les doc-tests à 0 — les portes sont
  bien réelles, log complet vérifié.)
- **Release v0.3.3.2 publiée** : 9 assets, checksums 8 entrées, downloads
  publics 200, tag `v0.3.3.2` poussé ; macOS repris de v0.3.3 (caveat
  runners, comme v0.3.3.1).
- **Edge Mania** : pin gabarit `build-cloison-edge.sh` v0.3.3.1 → **v0.3.3.2**,
  image reconstruite (`latest` = fe709d885bbc, backup `backup-20260909-200658`
  conservé, rollback documenté) ; tenant `demo-cloison` re-déployé (edge
  a6d9bfa37) ; **re-sonde VERTE** : restaurations exactes (Aminata Diop,
  email, tél ; ville = `[VILLE_SN]` par conception), **0 valeur claire** dans
  les logs edge, **0 échec NER**, **0 sentinelle brute** ; boot log
  `threshold=0.7` = le nouveau défaut v0.3.3.2 (v0.3.3.1 était 0.50) —
  confirmation que le binaire publié tourne ; dégradation
  `response_format` 400 réessayée vue en log (v0.3.3.1 conservé) ; 401
  cosmétiques des health checks gateway = dette Mania connue.
- **Docs déployées** : 11 pages 200 sur docs.wonkom.ai dont `manuel-n0.html`
  (nouveau) et la Q·9 FAQ ; sidebar mise à jour sur toutes les pages.
- **Leçons de session** : (a) `Get-Content`/`Set-Content` PowerShell
  double-encodent les UTF-8 → toujours `[System.IO.File]::ReadAllText/
  WriteAllText` explicite ; (b) les wrappers ssh.ps1 (ErrorActionPreference
  Stop) meurent sur le stderr de git/docker → ssh.exe direct avec
  `$ErrorActionPreference='Continue'` ; (c) jamais de commandes inline à
  guillemets — scripts fichiers scp'és (doctrine déjà établie, re-confirmée).

## 10. R2 — proposition de session conjointe (brouillon prêt)

Scénario : opencode multi-tours sur DeepSeek « thinking » à travers N0
v0.3.3.2. Critères : tours N≥2 = 200 ; **zéro sentinelle brute en sortie,
raisonnement inclus** ; restaurations exactes ; P5/P6 repris (streaming
SSE 22 chunks, observe-only). Prérequis client : réinstaller N0 en
`install-n0.sh --version v0.3.3.2` (jamais `latest`), ou attendre la mise à
jour du daemon Omarchy. Note I1 (§7) à faire valider par le pilote puis à
envoyer. Date : à proposer après validation pilote.

